---
import CtaBlog from '@components/CtaBlog.astro';
import { siteConfig } from '@utils/Config';
import { getCollection } from 'astro:content';
import AuthorCard from 'src/components/AuthorCard.astro';
import Layout from 'src/layouts/Layout.astro';
const autores= await getCollection('author');

---

<Layout title='Judith Blog'>
<section class="bg-gray-900">
    <div class="container px-6 py-10 mx-auto">
        <h1 class="text-3xl font-semibold capitalize lg:text-4xl text-white text-center">Los autores que hacen posible ByteMind</h1>
        <div class="grid grid-cols-1 gap-8 mt-8 md:mt-16 md:grid-cols-3">
            {autores.map(autor => (<AuthorCard author={autor.data} id={autor.id} />))}
         </div>
    </div>
</section>
<CtaBlog title="Explora nuestros artículos y tutoriales" />
</Layout>
